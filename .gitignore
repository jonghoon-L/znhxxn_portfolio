# 의존성 파일 (절대 올리면 안 됨)
node_modules/

# Next.js 빌드 결과물 (용량 큼)
.next/
build/

# 환경 변수 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 시스템 파일
.DS_Store
Thumbs.db